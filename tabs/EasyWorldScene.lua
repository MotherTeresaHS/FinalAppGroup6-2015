EasyWorldScene = class()

function EasyWorldScene:init(x)
    -- you can accept and set parameters here
    self.x = x
end

function EasyWorldScene:draw()
    -- Codea does not automatically call this method
end

function EasyWorldScene:touched(touch)
    -- Codea does not automatically call this method
end
